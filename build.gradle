apply plugin: 'java'

project.ext {
    cucumberVersion = '4.2.4'
}

dependencies {
    testCompile 'io.cucumber:cucumber-java:' + cucumberVersion
    testCompile 'io.cucumber:cucumber-junit:' + cucumberVersion
}

repositories {
    mavenCentral()
}
